<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_form_todo_task" model="ir.ui.view">
        <field name="name">TO-DO Task Form</field>
        <field name="model">todo.task</field>
        <field name="arch" type="xml">
            <form string="TO-Do task">
                <header>
                    <button name="do_toggle_done" type="object" string="Toggle Done" class="oe_highlight"/>
                    <button name="print_todo_task" string="一键生成" type="object"/>
                    <button name="print_todo_task1" string="生成箱单" type="object"/>
                    <button name="print_todo_task2" string="生成发票" type="object"/>
                    <button name="print_todo_task3" string="生成合同" type="object"/>
                    <button name="print_todo_task4" string="生成委托书" type="object"/>

                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="id"/>
                        <field name="is_done"/>
                        <field name="active" readonly="1"/>
                        <field name="drag_image_ids" widget="multidragndrop"  options="{'res_model_id': 'id','res_model_name':'todo.task'}"/>
                    </group>

                </sheet>
            </form>
        </field>
    </record>

    <record id="view_tree_todo_task" model="ir.ui.view">
     <field name="name">To–do Task Tree</field>
     <field name="model">todo.task</field>
     <field name="arch" type="xml">
       <tree colors="decoration–muted:is_done==True">
            <field name="name"/>
           <field name="is_done"/>
        </tree>
      </field>
    </record>

    <record id="view_filter_todo_task" model="ir.ui.view">
      <field name="name">To–do Task Filter</field>
      <field name="model">todo.task</field>
      <field name="arch" type="xml">
        <search>
          <field name="name" string="name"/>
            <field name="is_done" string="is_done"/>
          <filter string="Not Done"
            domain="[('is_done','=',False)]"/>
          <filter string="Done"
            domain="[('is_done','!=',False)]"/>
        </search>
      </field>
    </record>
</odoo>